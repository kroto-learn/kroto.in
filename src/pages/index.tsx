import { type NextPage } from "next";
import Landing from "../components/landing";

const Home: NextPage = () => {
  return (
    <div className="-mt-20">
      <Landing />
    </div>
  );
};

export default Home;
